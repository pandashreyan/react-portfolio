import { Canvas } from '@react-three/fiber';
import { Box } from '@react-three/drei';

const Scene3D = () => {
  return (
    <Canvas>
      {/* Basic rotating box */}
     
    </Canvas>
  );
};

export default Scene3D;